const ping = require('./ping');
const userLogin = require('./login');
const me = require('./me')
const topSecret = require('./topSecret');

module.exports = {
  ping,
  userLogin,
  me,
  topSecret,
};
